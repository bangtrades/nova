# Sprint Runs — Rolling Ledger

One row per shipped run — the backward-looking companion to the forward-looking `docs/SPRINT-N-tracker.md` files in the parent directory. Trackers show intent (what I committed to at sprint start); run summaries show truth (what actually landed, with what trade-offs, and what debt retired). This index is the scanner's entry point — sorted newest-first so the top of the table is always the most recent delivery.

Naming: run filenames are `S{sprint}-{slug}.md`. Each file carries a meta block at the top (Run ID, parent sprint, run window, delivery agents, status); the same fields feed the columns below. Run IDs follow `S{sprint}/R{seq}` — single-digit runs use `R01`, multi-story consolidations use ranges like `R03-04-09`.

The ledger is regenerated by hand (or via the `sprint-runner` skill's Operation 4) whenever a new run summary lands. Sort key is the run-window date, ties broken by Run ID descending.

| Run | Story / Scope | Landed | Status | One-line summary |
| --- | --- | --- | --- | --- |
| [S11-18-qa-audit](./S11-18-qa-audit.md) | S11-18 (QA) | 2026-04-22 | ✅ Delivered | Code-review-based three-axis QA audit (iPad landscape / dark mode / Dynamic Type) across all Tier 1 surfaces — sandbox can't boot an iPad simulator, so the sim walkthrough + screenshot capture stays on bang's Mac as a follow-up, but the *defect inventory* (the actual goal of the story) landed in-sandbox. **12 numbered architectural findings**, each with file + line + axis + proposed fix: only `LessonTileView` has `horizontalSizeClass` awareness app-wide; `EnhancedHomeView` lesson tiles hardcoded `.frame(width: 140)`; `LearningPathCard` hardcoded `.frame(width: 180)` **plus a latent math bug** — progress bar at line 69 multiplies by 160pt but the card's inner width after 16pt padding × 2 is 148pt, overflowing by 12pt at `progress=1.0`; `MasonryGrid` hardcodes `columns: Int = 2`; `TrophyRoomView` hardcoded to 3 `GridItem`s; `OnboardingView` + `KidsLoginView` + `FlipbookView` nav chrome + `DashyHintSheet` all have no `.frame(maxWidth:)` cap — content sprawls edge-to-edge on iPad; **`NovaPalette.displayFont(size:)` missing `relativeTo:` parameter** — Bangers is a fixed-size PostScript lookup so every title stays at design-time size regardless of user accessibility setting; `.lineLimit(1)` without `.minimumScaleFactor()` at 5 sites; dark-mode contrast risk on `.white` foreground atop `pathColor` + `Category.blue`/`Category.purple` gradients (WCAG AA re-verify). **`NovaPalette` adaptive implementation verified ✓** — `Color(light:dark:)` uses `UIColor(dynamicProvider:)` reading `UITraitCollection.userInterfaceStyle` → adaptive at *runtime*, so dark-mode risk is contained to specific `.white`-on-color hotspots rather than systemic. Grouped into three proposed Sprint 12 stories: `S12-01` iPad layout pass (5 pts) · `S12-02` Dynamic Type pass (2 pts) · `S12-03` Dark-mode contrast pass (1 pt). iPad risk is now **known as 12 discrete items** rather than unknown unknowns — the point of a pre-demo QA sweep. Mac-only follow-up: `xcodebuild -destination 'platform=iOS Simulator,name=iPad Pro (12.9-inch)'` walkthrough + screenshot capture into `docs/sprint-runs/S11-18-qa-screenshots/`. MX + QA epics close Sprint 11 at **93/93 (100%)**. |
| [S11-16-reduce-motion-audit](./S11-16-reduce-motion-audit.md) | S11-16 (MX) | 2026-04-22 | ✅ Delivered | Swept all 52 `withAnimation(…)` sites across 16 files in `src/Apps/NovaKids/Sources/Views/` and classified each into three buckets: **ambient motion — gate at source** (ExperimentCardView correct-drop snap + wrong-drop bounce + auto-dismiss fade + ShakeModifier driver guarded at `shakeAnimation = !reduceMotion` source so the Timer-based ±10pt jitter sink never fires under reduce-motion — retired a latent pre-S11-16 bug; FlipbookView Prev/Next card-swap turns into instant state flip under reduce-motion while progress dots + content still convey navigation semantically; DashyView `updateAnimationFromEmotion` showCelebration gated both on enter and exit because per-response emotion celebration is ambient motion fired many times per chat session — distinct from ConfettiView's once-per-experiment semantics); **discrete-event celebration — carve-out and document** (ConfettiView header doc now carries an explicit "S11-16 carve-out" paragraph codifying *why* particle physics deliberately survive reduce-motion per Apple HIG motion-semantics — one-shot "you did it" beat ≠ ambient motion; pattern sibling to QuizPowReaction's internal `if reduceMotion { … return }` degraded path and BadgeUnlockBurst's same shape, all three now explicitly documented as celebration views); **already gated upstream — verified** (12 files confirmed compliant with no edit — WelcomeHeader `guard !reduceMotion else { return }` at task entry makes wave-loop sites unreachable; QuizPowReaction + BadgeUnlockBurst internal degraded-path branches; BadgeView `guard earned, !reduceMotion` before spring; QuizCardView 5 ternary-gated sites; VoiceCardView 3 sites across if/ternary/if-else bifurcation; StoryCardView + DashyHintButton ternaries; DashyCharacterView ~10 repeatForever idle loops every one inside `if !reduceMotion { }`; ParentalGateView 3 sites; KidsLoginView + OnboardingView from S11-17). Canonical gate shape locked: `withAnimation(reduceMotion ? nil : .spring(...))` — single-sited body closure so future edits land in one place; `nil` is the type-correct form since signature is `Animation?`. Three-bucket triage is reproducible — `grep -rn withAnimation` cross-referenced against the run-summary file table is the Sprint 12+ regression test. Closes MX epic at 10/10 (100%); Sprint 11 Total flips to **90/93 (97%)** — only S11-18 (iPad landscape + dark + DynamicType QA, 3 pts) remains. |
| [S11-17-auth-onboarding-polish](./S11-17-auth-onboarding-polish.md) | S11-17 (QA) | 2026-04-22 | ✅ Delivered | First-impression surfaces — Sign-In + 4-page Onboarding — brought onto the 3+1 palette + `Spacing` enum + `.novaPrimary()`/`.novaSecondary()` + reduce-motion-at-source vocabulary. KidsLogin: `novaBlue→novaPurple` logo gradient → `coral→sun` + 2pt ink-outline + ink glyphs (AAA contrast); four floating shapes flipped to **deliberate weight-variance** pattern (ink 0.08 / coral 0.18 / ink 0.06 / sun 0.28) over flat `.page`; `AnimatedBackgroundView` wired with source-level `.onAppear` reduce-motion guard — shapes lock at neutral offset when motion-off; **Sign In with Apple stays stock per Apple HIG** (App Review risk + `ASAuthorizationAppleIDButton` contract — no `.novaPrimary()` re-skin attempt). Onboarding: `@Environment(\.accessibilityReduceMotion)` with **confetti carve-out doc** (one-shot discrete-event celebration preserved regardless of motion setting per Apple HIG motion-semantics guidance); `meetDashyPage` refactored from silently-broken inline `sin(Date().timeIntervalSince1970)` (which sampled once per body eval — animation froze after first frame) into `TimelineView(.animation)`-driven `DashyIdlePose` struct + `.rest` fallback + `dashyPageContent(pose:)` ViewBuilder helper (ternary-on-`TimelineView`-Schedule fails because `ExplicitTimelineSchedule`/`AnimationTimelineSchedule` are distinct concrete types — `@ViewBuilder`-branch at function level instead, bonus: reduce-motion path instantiates zero TimelineView); `transitionToPage(_:)` helper centralises reduce-motion on the 4-page flow — one decision point; all 7 onboarding buttons routed through `.novaPrimary()`/`.novaSecondary()` (stripping the bespoke `novaOrange→novaPurple` "Let's Go!" gradient — confetti carries the celebration, button stays on shared DS); 46 `Spacing.*` call sites across both files. Preserved carve-outs: Dashy body `novaPurple` (character identity from S11-10), avatar-picker per-avatar colors (`Category.*` semantic identity, not chrome drift), `novaCardBackground` (adaptive card-fill alias, semantically distinct from `.page`). Retired AC "two raw `.font(.title)` sites" — grep confirmed only 1 remaining across all of NovaKids, inside `NovaCard.swift`'s `#Preview` (already swept during S11-02/S11-04). QA epic opens at 4/7 (57%); Sprint 11 Total flips to **88/93 (95%)**. |
| [S11-19-live-data-wireup](./S11-19-live-data-wireup.md) | S11-19 (MVP) | 2026-04-22 | ✅ Delivered | Five ViewModels (Home / Lessons / Flipbook / Trophy / Dashy) flipped from mock arrays to real `APIRouter` calls via idempotent `attach(apiRouter:)` / `attach(apiRouter:childId:)`; zero-arg `init()` preserved for `#Preview` parity; `@Published var loadError: APIError?` surfaces fetch failures as unified ink-stroke + page-fill banners with `.novaSecondary()` Try Again above cached content (not in place of it); `NovaKidsApp` reads backend base URL from `NOVA_BACKEND_HOST` env var (iPad-on-LAN demo loop — flip Mac IP without recompile, localhost fallback for simulator); `async let` parallel fan-outs for known fan counts (paths+lessons; catalog+earned; home's 3-fan); `loadCardsIfNeeded()` lazy pattern on Flipbook because the list endpoint returns summaries; wire literal `role == "sparky"` + `/sparky/chat` path preserved as S12 coordinated-rename carve-out; `GET /lessons` silent querystring bug fixed (`request.params` → `request.query` — Zod was validating into `.query`, code was reading empty `.params`, all list calls silently defaulted to page 1 with no filter). MVP epic closes 8/8 (100%); Sprint 11 Total flips to 84/93 (90%). |
| [S11-13-dashy-chat](./S11-13-dashy-chat.md) | S11-13 (T2) | 2026-04-22 | ✅ Delivered | `DashyView` rebuilt on DS primitives — header type scale (`NovaPalette.headingFont(size: 22)`) + 20-min session-time progress bar (`TimelineView(.periodic)` + `GeometryReader`, 1s tick or 5s under reduce-motion) + **coral child bubble vs paper-and-ink Dashy bubble** (semantic "child = coral actor, Dashy = ink-on-page") + `.novaSecondary()` on every suggestion/starter pill + `TimelineView`-based thinking dots (0.3s) and pulse ring (30fps) with reduce-motion fallbacks + error card rebuilt on 20pt rounded rect + ink stroke + `.novaPrimary()` Try Again; VM contract preserved byte-for-byte (wire literal `role == "sparky"` kept as asymmetric-rename carve-out for S12). T2 epic closed at 18/18 (100%). |
| [S11-12-lessons-pinterest](./S11-12-lessons-pinterest.md) | S11-12 (T2) | 2026-04-22 | ✅ Delivered | `LessonTileView` rebuilt on DS chrome — ink outline + difficulty-coded accent stripe (green/orange/purple/coral) + page background + iPad-only hover lift + 7-day NEW badge (sun capsule + Bangers ink) + coral completion checkmark in opposite corner; `PathFilterButton` → `PathFilterPill` (ink-outline default, coral-fill selected); all padding via `Spacing` enum; `MasonryGrid` math preserved byte-for-byte. |
| [S11-haptic-ladder-sweep](./S11-haptic-ladder-sweep.md) | S11-15 (MX) | 2026-04-22 | ✅ Delivered | Every inline `UIImpactFeedbackGenerator` / `UINotificationFeedbackGenerator` on Tier 1 routed through `NovaHaptics.tap / .commit / .success / .wrong`; 11 call sites across 6 files, one semantic bug fix (ExperimentCard wrong-drop was on `.light` / acknowledge rung). |
| [S11-flipbook-skeletons](./S11-flipbook-skeletons.md) | S11-11 + S11-14 (T2 + MX) | 2026-04-22 | ✅ Delivered | Flipbook chrome (header + progress dots + nav buttons) rebuilt on the 3+1 palette + Bangers card-type label + `NovaSecondaryButtonStyle` Prev/Next; `LoadingSkeletonView` wired across Lessons + Trophy VMs; Flipbook VM audited and documented as S12 carve-out. |
| [S11-dashy-reskin](./S11-dashy-reskin.md) | S11-10 (T2) | 2026-04-21 | ✅ Delivered | Dashy character reskin on comic palette + silhouette stroke; `DashySpeechBubble` component built and wired into dialogue call sites; identity complete — the mascot is the mascot everywhere. |
| [S11-nav-consistency](./S11-nav-consistency.md) | S11-08 (T2) | 2026-04-21 | ✅ Delivered | `NovaNavigationStyle` view modifier introduced; applied across Home / Quiz / Trophy; one ink-on-page navigation language app-wide. |
| [S11-trophy-refinement](./S11-trophy-refinement.md) | S11-07 (T1) | 2026-04-21 | ✅ Delivered | `BadgeView` tile rebuilt on DS primitives; `ProgressRing` extracted into DS; `BadgeUnlockBurst` reaction component; TrophyRoom + BadgeDetailSheet reads as a gallery. |
| [S11-quiz-comic-ification](./S11-quiz-comic-ification.md) | S11-06 (T1) | 2026-04-21 | ✅ Delivered | QuizCardView rebuilt on DS primitives; 469-line monolith split into four files with clean separation of concerns; latent `dismiss()`-pops-the-lesson bug fixed; `NovaHaptics` 4-rung ladder landed proactively (so S11-15 collapses to pure call-site replacement); POW! reaction with reduce-motion fallback. |
| [S11-home-refresh](./S11-home-refresh.md) | S11-05 (T1) | 2026-04-20 | ✅ Delivered | Home tab + Enhanced-Home tab consume S11-03/04 DS primitives; three latent correctness bugs caught and fixed en route; Swift 6 strict-concurrency Timer hazard in `WelcomeHeader` neutralized; `LoadingSkeletonView` wired into Home refresh path. |
| [S11-ds-dashy-rename](./S11-ds-dashy-rename.md) | S11-03 + S11-04 + S11-09 (DS) | 2026-04-20 | ✅ Delivered | `NovaCard` + `NovaPrimaryButtonStyle` + `NovaSecondaryButtonStyle` built; Bangers display font plumbed with system fallback; Sparky → Dashy iOS rename complete; DS epic 100% — T1 screens unblocked. |
| [S11-palette-spacing](./S11-palette-spacing.md) | S11-01 + S11-02 (DS) | 2026-04-20 | ✅ Delivered | 3+1 palette (ink / coral / sun / page) + category rainbow consolidated in `NovaPalette`; `Spacing` enum introduced; foundation in place for Tier 1 rebuilds. |
| [S10-skill-engine-s10-12](./S10-skill-engine-s10-12.md) | S10-12 | 2026-04-18 | ✅ Shipped | `childContextBuilder` + `skillRouter` + Zod-retry + `generateCardsWithSkills` + orchestrator wiring + Dev Console pipeline trace + 44 new tests; decomposition-aware skill path with legacy fallback. |
| [S10-skill-engine-s10-07](./S10-skill-engine-s10-07.md) | S10-07 | 2026-04-18 | ✅ Shipped | quiz-maker skill defs + per-skill Zod validator + modality partial + engine wiring + Dev Console Skills tab polish + tests. |
| [S10-skill-engine-s10-06](./S10-skill-engine-s10-06.md) | S10-06 | 2026-04-18 | ✅ Shipped | story-writer skill def bundle (manifest + prompt + age profiles + styles + topics) + Handlebars-based registry + loader + Dev Console Skills tab. |
| [S10-skill-engine-kickoff](./S10-skill-engine-kickoff.md) | S10-06/07 spike | 2026-04-17 | ✅ Delivered | Skill-engine design spike — loader + story-writer + quiz-maker shapes; 6 decisions locked including sliding-scale effective age; unblocks R4 implementation. |
| [S10-guidance-context](./S10-guidance-context.md) | S10-04 + S10-05 | 2026-04-17 | ✅ Delivered | Parent guidance model + timezone-aware session context engine + pipeline prompt injection + Dev Console Parent Guidance + Session Context tabs; single-day delivery. |
